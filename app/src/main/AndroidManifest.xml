<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApp"
        tools:targetApi="31">
        <activity
            android:name=".andoidMaster.superheroapp.SuperHeroListActivity"
            android:exported="false" />
        <activity
            android:name=".andoidMaster.todoApp.TodoActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.todoapp" />
        <activity
            android:name=".andoidMaster.imcCalulator.ResultIMCActivity"
            android:exported="false" />
        <activity
            android:name=".andoidMaster.imcCalulator.ImcCalculatorActivity"
            android:exported="false" />
        <activity
            android:name=".andoidMaster.MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".andoidMaster.firstApp.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".andoidMaster.firstApp.FirstAppActivity"
            android:exported="true" />
    </application>

</manifest>